import React from 'react';
import CarSelect from '../Things/SelectCar';

const HistoryInfo = ({ }) => {
  return (
    <div>
        <CarSelect /> 
    </div>
  );
};

export default HistoryInfo;